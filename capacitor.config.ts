import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'react.ionic.crossplatform',
  appName: 'react-ionic-crossplatform',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
